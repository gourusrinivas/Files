pipeline {
    agent { label 'Slave' }

    stages {
        stage('Python') {
        parallel {
        stage('Single') {
        steps {
        bat "S_with_json.py"
            }
        }
        stage('Single1') {
        steps {
        bat "S_with_json.py"
        }
        }
        }
       }
    }
}
